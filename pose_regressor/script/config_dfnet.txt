############################################### Mega-NeRF training example  ###############################################
model_name=dfnet
expname=dfnet
basedir=../logs/high-simple       # Change this if change scene
datadir=../../data/high-simple       # Change this if change scene
dataset_type=mega_nerf
trainskip=1 # train             # Skip 1/trainskip data
testskip=1 # train              # Skip 1/trainskip data
df=2                            # Downscale the image while rendering using mega-nerf
epochs=200                      # Number of epochs to train
tinyimg=True
DFNet=True
featurenet_batch_size=3         # Batch size, 4 or 8
random_view_synthesis=True
rvs_refresh_rate=20             # num_epoch every rendering new views
rvs_trans=0.05                  # translation range for random view synthesis
rvs_rotation=8                  # rotation range for random view synthesis
d_max=1                         # max translation range for calculate bounding box of random view synthesis
eval=False

## mega-nerf settings below:
exp_name=simple                                        # Change this if change scene
config_file=../mega-nerf-output/configs/yaml/high-simple.yaml        # Change this if change scene
container_path=../high-simple-merged                   # Change this if change scene
